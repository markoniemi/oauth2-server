spring:
  application:
    name: auth-server
  config:
    import: optional:classpath:/config/application.yaml
  security:
    oauth2:
      authorizationserver:
        issuer: http://localhost:9000
        client: {}

server:
  port: 9000
  servlet:
    context-path: /

app:
  security:
    users: []
